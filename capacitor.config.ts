import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Aura Budget Mobile',
  webDir: 'dist',
  ios: {
    scrollEnabled: false
  }
};

export default config;
